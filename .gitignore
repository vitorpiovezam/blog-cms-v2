# Serverless deploy artifacts
.serverless

# Go build artifacts (make build / make package)
.bin

# Stray binary — Go outputs module-name binary when `go build .` runs without -o
blog-cms-v2
node_modules
