{
  "name": "blog-cms-v2",
  "version": "1.0.0",
  "description": "Blog CMS API — Go on AWS Lambda (provided.al2023)",
  "scripts": {
    "build": "make build",
    "offline": "make offline",
    "deploy": "make deploy"
  },
  "devDependencies": {
    "serverless": "^3.0.0",
    "serverless-offline": "^13.0.0"
  }
}
